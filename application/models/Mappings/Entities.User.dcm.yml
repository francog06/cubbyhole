Entities\User:
  type: entity
  table: user
  uniqueConstraints:
    email_index:
      columns:
        - email
  fields:
    id:
      type: integer
      id: true
      generator:
        strategy: AUTO
    email:
      type: string
      length: 255
      nullable: false
    password:
      type: string
      length: 255
      nullable: false
    registration_date:
      type: datetime
      nullable: false
    user_location_ip:
      type: string
      nullable: false
    is_admin:
      type: boolean
      nullable: false
  oneToMany:
    plan_historys:
      targetEntity: PlanHistory
      mappedBy: user
      cascade: ["persist", "merge"]
    folders:
      targetEntity: Folder
      mappedBy: user
      cascade: ["persist", "merge"]
    files:
      targetEntity: File
      mappedBy: user
      cascade: ["persist", "merge"]
    shares:
      targetEntity: Share
      mappedBy: user
      cascade: ["persist", "merge"]
    